3009:
    DONE > copy code test -> extract_data -> extract lại
    DONE > xem lại cách lấy context (không dùng so sánh similarity, sử dụng db.as_retriever())
    DONE > thêm vào sys_prompt logic kiểu: VI. Lưu đồ => VI. Lưu đồ quá trình phát triển phần mềm. + sửa lại phân cấp chương (#) (chỗ hồ sơ bị sai, all file)
        => Rõ ràng nội dung / lưu đồ ... của văn bản nào => tối ưu truy xuất.
0610:
    xem xét sửa cách lấy context khác thay vì k văn bản gần nhất (ContextualCompressionRetriever - Contextual Retrieval) -> gọi API nhiều lần cho 1 lần chạy
    hình như chưa chunking (somehow kh cần làm) -> thinking....
    thêm vào logic: prompt có kèm ảnh hay không => sửa qa_chain.
        không ảnh: đưa câu hỏi + context dựa trên câu hỏi vào prompt
        có ảnh: đưa ảnh + câu hỏi + context dựa trên câu hỏi vào prompt
    đánh giá hệ thống -> ragas -> làm data dạng text: (questions, contexts, answers, ground_truths) -> openai_api_key
                                                ảnh: .....
    OCR thông qua api (gemini)
    mỗi api 1 tác vụ (api cho sinh câu lời, api cho embedding, ....)
    xem lại chỗ tách từ (recursivecharactertextsplitter / semantic chunker)

